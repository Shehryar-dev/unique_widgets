import '../../../constants/screen_path.dart';

class SearchPage extends StatelessWidget {
  const SearchPage();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🔍 Search Page', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
    );
  }
}